<section class="deepscan">
  <div class="container">
    <h2 class="section-title">Built with Next-Generation <span class="gradient-text">DeepScan</span></h2>
    <p class="section-subtitle">
      Our proprietary technology combines edge computing, behavioral heuristics, and automated reporting for maximum protection.
    </p>

    <div class="deepscan-grid">
      <div class="deepscan-card">
        <div class="deepscan-icon">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#00E676" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <rect x="2" y="3" width="20" height="14" rx="2" ry="2"/>
            <line x1="8" y1="21" x2="16" y2="21"/>
            <line x1="12" y1="17" x2="12" y2="21"/>
          </svg>
        </div>
        <h3>Security Primitives — Edge Computing Analysis</h3>
        <p>Most of our analysis happens locally on edge nodes close to you, minimizing data transit time and maximizing security.</p>
      </div>

      <div class="deepscan-card">
        <div class="deepscan-icon">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#00E676" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M12 2a10 10 0 1 0 10 10H12V2z"/>
            <path d="M12 2a10 10 0 0 1 10 10"/>
            <circle cx="12" cy="12" r="3"/>
          </svg>
        </div>
        <h3>Behavioral Heuristics</h3>
        <p>We look beyond the image. Our AI understands the context of digital interaction, identifying behaviors typical of romance or crypto scammers.</p>
      </div>

      <div class="deepscan-card">
        <div class="deepscan-icon">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#00E676" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
            <polyline points="14 2 14 8 20 8"/>
            <line x1="16" y1="13" x2="8" y2="13"/>
            <line x1="16" y1="17" x2="8" y2="17"/>
            <polyline points="10 9 9 9 8 9"/>
          </svg>
        </div>
        <h3>Automated Reporting</h3>
        <p>When a scam is confirmed, VerifyAI can automatically generate report drafts for relevant authorities like the FTC or local cybercrime units.</p>
      </div>
    </div>

    <a href="/SecuritySafe/#features" class="back-link">
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="15 18 9 12 15 6"/></svg>
      Back to Overview
    </a>
  </div>
</section>

<style>
  .deepscan {
    padding: 100px 0;
  }

  .deepscan-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
    margin-bottom: 40px;
  }

  .deepscan-card {
    padding: 36px 28px;
    background: var(--bg-card);
    border: 1px solid var(--border-color);
    border-radius: 16px;
    transition: all 0.3s;
  }

  .deepscan-card:hover {
    border-color: rgba(0, 230, 118, 0.3);
    transform: translateY(-4px);
  }

  .deepscan-icon {
    width: 52px;
    height: 52px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(0, 230, 118, 0.1);
    border-radius: 12px;
    margin-bottom: 20px;
  }

  .deepscan-card h3 {
    font-size: 1.1rem;
    font-weight: 700;
    margin-bottom: 12px;
    line-height: 1.4;
  }

  .deepscan-card p {
    color: var(--text-secondary);
    font-size: 0.9rem;
    line-height: 1.7;
  }

  .back-link {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    color: var(--primary);
    font-weight: 600;
    font-size: 0.95rem;
    transition: gap 0.2s;
  }

  .back-link:hover {
    gap: 12px;
  }

  @media (max-width: 768px) {
    .deepscan-grid {
      grid-template-columns: 1fr;
    }
  }
</style>